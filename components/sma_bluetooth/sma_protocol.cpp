/* MIT License — based on work by Lupo135, darrylb123, keerekeerweere */

#include "sma_protocol.h"

// sma_protocol.cpp is intentionally minimal — most protocol helpers are
// inline in the header.  This file exists so the build system has a .cpp
// to compile for the component and to anchor any non-inline symbols if
// needed in the future.

namespace esphome {
namespace sma_bluetooth {

// Placeholder — all current helpers are header-only (inline).

}  // namespace sma_bluetooth
}  // namespace esphome
